<!DOCTYPE html>
<html>
  <head>
    <title>Blocipedia</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <ul class="nav nav-tabs">
        <li><%= link_to "Home", welcome_index_path %></li>
        <li><%= link_to "About", welcome_about_path %></li>
        <li><%= link_to "Support", welcome_support_path %></li>
        <div>
          <small class="pull-right">
            <% if user_signed_in? %>
              Signed in as: <strong><%= current_user.email %></strong><br>
              <%= link_to 'Edit profile', edit_user_registration_path, class: 'pull-right' %><br>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'pull-right' %><br>
            <% else %>
              <%= link_to "Sign In", new_user_session_path  %>
            <% end %>
          </small>
        </div>
      </ul>
      <%= yield %>
    </div>
  </body>
</html>
